# Week 3 Study Roadmap

**3 August to 9 August 2026**

---

## Monday 3 August

| Time | Task |
|------|------|
| 4:00–5:00 | React — Install Tailwind CSS in your project. Convert your chat UI from plain CSS to Tailwind. Make it look clean and modern. |
| 5:00–6:00 | AI — Learn what LangChain agents are (concept). Build a basic agent that decides which tool to use based on user input. |
| 6:00–6:45 | Go — Learn Go error handling properly. Rewrite your REST API with proper error responses and status codes. |

---

## Tuesday 4 August

| Time | Task |
|------|------|
| 4:00–5:00 | React — Build a responsive navbar with Tailwind. Mobile hamburger menu + desktop links. Make it work on both screen sizes. |
| 5:00–6:00 | AI — Give your agent a web search tool using LangChain's `DuckDuckGoSearchRun`. It should search the web when it doesn't know the answer. |
| 6:00–6:45 | Go — Learn Go structs and JSON marshaling. Build an endpoint that accepts a JSON body and returns a structured JSON response. |

---

## Wednesday 5 August

| Time | Task |
|------|------|
| 4:00–5:00 | React — Style your todo app fully with Tailwind. Add hover states, transitions, and a clean color scheme. Should look portfolio-worthy. |
| 5:00–6:00 | AI — Combine your RAG pipeline + agent. The agent should first search your ChromaDB, and if no answer found, fall back to web search. |
| 6:00–6:45 | Go — Learn Go middleware chaining. Add both a logger and a basic API key auth middleware to your server. |

---

## Thursday 6 August

| Time | Task |
|------|------|
| 4:00–5:00 | React — Deploy your React app to **Vercel**. Sign up, connect GitHub repo, deploy in one click. Share the live link. |
| 5:00–6:00 | AI — Deploy your Flask RAG API to **Render**. Set environment variables (OpenAI key). Test your live `/ask` endpoint with Postman. |
| 6:00–6:45 | Go — Write unit tests for your Go API using Go's built-in `testing` package. Test at least 2 routes. |

---

## Friday 7 August

| Time | Task |
|------|------|
| 4:00–5:00 | React — Connect your deployed React app (Vercel) to your deployed Flask API (Render). Fix any CORS issues. Full live app working end to end. |
| 5:00–6:00 | AI — Add a loading state and error handling to your agent. If web search fails, it should gracefully return a fallback message. |
| 6:00–6:45 | Go — Deploy your Go service to Render as a separate web service. Test it via the live URL. |

---

## Saturday 8 August

| Time | Task |
|------|------|
| 4:00–5:30 | React + AI — Polish your portfolio chat app. Add a header, a subtle animation on messages appearing, and a "Powered by AI" badge. Make it something you're proud to show. |
| 5:30–6:45 | Go — Connect your live Go microservice to your Flask backend. Full 3-service architecture running live: React → Flask → Go. |

---

## Sunday 9 August

| Time | Task |
|------|------|
| 4:00–5:00 | Polish — Go through all 3 deployed services. Fix any bugs, broken links, or UI issues. |
| 5:00–6:00 | GitHub — Update all READMEs with live links. Add screenshots of your app to the README. This is what interviewers see. |
| 6:00–6:45 | Reflect — Write down what you built across 3 weeks. You now have a live, deployed, full-stack AI app. Note what Week 4 should focus on. |

---

## Deliverables by end of Week 3 (9 August)
- A fully styled React app with Tailwind deployed on Vercel
- A Flask RAG API with LangChain agents deployed on Render
- A Go microservice deployed and connected
- A live shareable URL for your portfolio project
- GitHub READMEs with screenshots

---

**Week 4 preview:** LangChain memory types, multi-document RAG, deeper React with custom hooks, and starting to think about your main portfolio project idea.
