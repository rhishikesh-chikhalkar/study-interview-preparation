# Week 1 Study Roadmap

**13 July to 19 July 2026**

---

## Monday 13 July

| Time | Task |
|------|------|
| 4:00–5:00 | React — Watch a 1hr intro: *"React in 1 hour"* by Traversy Media on YouTube. Just watch, don't code yet. |
| 5:00–6:00 | AI — Read what LangChain is + install it. Run `pip install langchain openai`. Get an OpenAI API key. |
| 6:00–6:45 | Go — Install Go, run "Hello World", read what goroutines are (just concepts today). |

---

## Tuesday 14 July

| Time | Task |
|------|------|
| 4:00–5:00 | React — Set up your first project via `npx create-react-app`. Build a component that displays your name and a button. |
| 5:00–6:00 | AI — Follow LangChain quickstart. Make a script that takes a question and returns an answer using OpenAI. |
| 6:00–6:45 | Go — Write a program that adds two numbers, loops through a list, and prints output. |

---

## Wednesday 15 July

| Time | Task |
|------|------|
| 4:00–5:00 | React — Learn `useState`. Build a counter app (increment, decrement, reset). |
| 5:00–6:00 | AI — Add memory to your Day 2 chatbot so it remembers previous messages in a conversation. |
| 6:00–6:45 | Go — Build a simple function that takes a name and returns a greeting. Learn what a struct is. |

---

## Thursday 16 July

| Time | Task |
|------|------|
| 4:00–5:00 | React — Learn `useEffect`. Fetch a public API (e.g. `https://catfact.ninja/fact`) and display the result on screen. |
| 5:00–6:00 | AI — Make your chatbot take input from the terminal in a loop (a real back-and-forth conversation). |
| 6:00–6:45 | Go — Build a basic HTTP server that returns "Hello from Go" when you hit `localhost:8080`. |

---

## Friday 17 July

| Time | Task |
|------|------|
| 4:00–5:00 | React — Build a simple todo app (add item, mark done, delete). Uses useState fully. |
| 5:00–6:00 | AI — Connect your chatbot to a PDF. Use LangChain's PDF loader so it can answer questions about a document. |
| 6:00–6:45 | Go — Add a second route to your server (`/about`). Learn what a Go function with multiple return values looks like. |

---

## Saturday 18 July

| Time | Task |
|------|------|
| 4:00–5:30 | React — Refactor your todo app. Split into 3 components: `App`, `TodoInput`, `TodoList`. Learn props by passing data between them. |
| 5:30–6:45 | AI — Make your PDF chatbot respond via a simple Flask API endpoint instead of terminal. This bridges AI + backend. |

---

## Sunday 19 July

| Time | Task |
|------|------|
| 4:00–5:00 | Review — Go through everything you built. Fix anything broken. Push all code to GitHub. |
| 5:00–6:00 | Go — Build a basic HTTP server that returns "Hello from Go". Add a second route `/about`. |
| 6:00–6:45 | Rest / light reading — Search "AI SaaS app ideas 2026". Note 3 ideas you'd like to build someday. |

---

## Deliverables by end of Week 1 (19 July)
- A working React todo app with components and props
- A working AI chatbot that reads PDFs via Flask
- A basic Go web server with multiple routes
- All code pushed to GitHub

---

**Week 2 starts Monday 20 July**
